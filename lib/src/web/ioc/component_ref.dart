part of game_store.web;

/** Annotation used to reference polymer elements. */
class ComponentRef {
  final String htmlUrl;
  final String tagName;
  const ComponentRef(this.htmlUrl, this.tagName);
}
